#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <math.h>

#define MX 100000
#define MOD 1000000009

bool isPowerOf2(int f) { return(f && !(f & (f - 1)));}
char getC() { char f = getchar(); return(f); }
void putC(char c) { putchar(c); }
void getMod(int *v) { *v = (*v%10); }
int main()
{

    return(0);
}
